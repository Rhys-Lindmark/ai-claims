export type ProvenanceField = 'retrieved_at' | 'content_digest' | 'rights_note' | 'reviewed_by' | 'dependent_finding_ids';
export interface EvidenceProvenanceRecord { source_id: string; title: string; retrieved_at: string | null; content_digest: string | null; rights_note: string | null; reviewed_by: string | null; dependent_finding_ids: string[]; }
export function missingProvenance(record: EvidenceProvenanceRecord): ProvenanceField[] { const missing: ProvenanceField[] = []; if (!record.retrieved_at) missing.push('retrieved_at'); if (!record.content_digest) missing.push('content_digest'); if (!record.rights_note?.trim()) missing.push('rights_note'); if (!record.reviewed_by?.trim()) missing.push('reviewed_by'); if (!record.dependent_finding_ids.length) missing.push('dependent_finding_ids'); return missing; }
export function findingProvenanceDecision(findingId: string, records: EvidenceProvenanceRecord[]) { const cited = records.filter((record) => record.dependent_finding_ids.includes(findingId)); const incomplete = cited.map((record) => ({ source_id: record.source_id, missing: missingProvenance(record) })).filter((item) => item.missing.length); return { finding_id: findingId, cited_source_count: cited.length, complete_source_count: cited.length - incomplete.length, can_publish: cited.length > 0 && incomplete.length === 0, incomplete_sources: incomplete }; }

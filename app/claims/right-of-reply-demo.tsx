import fixture from '@/data/correction-request-fixture.json';

export function RightOfReplyDemo() {
  const request = fixture.request;
  const stages = ['received', 'identity check', 'editorial review', 'partially accepted', 'published in v3'];
  return <section className="border-t-2 border-ink bg-[#ded8ca]"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><p className="font-mono text-[9px] font-bold uppercase text-cobalt">Right of reply · synthetic</p><div className="mt-2 grid gap-6 lg:grid-cols-[1fr_auto] lg:items-end"><div><h2 className="text-4xl font-black tracking-[-0.05em]">Open to correction. Not captured by it.</h2><p className="mt-3 max-w-2xl text-sm leading-relaxed text-ink/60">Speakers and publishers can supply context, evidence, and a public reply. Identity and attachments are reviewed, response timing is visible, and editors retain independent control over the analysis.</p></div><span className="bg-acid px-3 py-2 font-mono text-[9px] font-bold uppercase">Response due · {request.response_due_at.slice(0, 10)}</span></div><div className="mt-7 grid grid-cols-2 gap-1 sm:grid-cols-5">{stages.map((stage, index) => <div className={`border border-ink p-3 text-center font-mono text-[8px] font-bold uppercase ${index === stages.length - 1 ? 'bg-cobalt text-white' : 'bg-paper'}`} key={stage}>{stage}</div>)}</div><div className="mt-5 grid gap-4 lg:grid-cols-2"><article className="border-2 border-ink bg-paper p-5"><div className="flex flex-wrap items-center justify-between gap-2"><span className="font-mono text-[8px] font-bold uppercase text-coral">Requester context</span><span className="font-mono text-[8px] font-bold uppercase">{request.requester_role} · {request.identity_state}</span></div><p className="mt-4 text-sm font-semibold leading-relaxed">{request.requested_context}</p><div className="mt-4 border-t border-ink/15 pt-3"><p className="font-mono text-[8px] font-bold uppercase">Public reply</p><p className="mt-2 text-xs leading-relaxed text-ink/60">{request.public_reply}</p></div><p className="mt-4 font-mono text-[8px] uppercase text-ink/40">{request.attachments.length} reviewed attachment · {request.target_record_ids.length} target records</p></article><article className="border-2 border-ink bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-2"><span className="font-mono text-[8px] font-bold uppercase text-cobalt">Editorial response</span><span className="bg-[#ffd76a] px-2 py-1 font-mono text-[8px] font-bold uppercase">{request.triage_state.replaceAll('_', ' ')}</span></div><p className="mt-4 text-sm font-semibold leading-relaxed">{request.editorial_response}</p><div className="mt-4 border-t border-ink/15 pt-3"><p className="font-mono text-[8px] font-bold uppercase">Independence rule</p><p className="mt-2 text-xs leading-relaxed text-ink/60">{request.independence_notice}</p></div><p className="mt-4 font-mono text-[8px] uppercase text-ink/40">Result recorded in {request.resulting_version_id}</p></article></div><p className="mt-4 font-mono text-[8px] font-bold uppercase text-ink/40">Fixture only · no real correction request represented</p></div></section>;
}


import fixture from '@/data/evidence-ledger-fixture.json';

const tone: Record<string, string> = { supports: 'bg-acid', contradicts: 'bg-coral', qualifies: 'bg-[#ffd76a]', context_only: 'bg-ink/10' };

export function EvidenceLedgerDemo() {
  return <section className="border-t-2 border-ink bg-white"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><p className="font-mono text-[9px] font-bold uppercase text-coral">Evidence ledger · synthetic</p><h2 className="mt-2 max-w-4xl text-4xl font-black tracking-[-0.05em]">Sources do different jobs.</h2><p className="mt-3 max-w-2xl text-sm leading-relaxed text-ink/60">Citation presence is not evidence strength. Each link records direction, exact context, relevance, source quality, dates, confidence, and review state before a verdict is considered.</p><div className="mt-7 border-2 border-ink bg-paper p-5"><span className="font-mono text-[8px] font-bold uppercase text-cobalt">{fixture.canonical_claim.canonical_claim_id}</span><h3 className="mt-2 text-2xl font-black">{fixture.canonical_claim.text}</h3></div><div className="mt-4 grid gap-4 lg:grid-cols-3">{fixture.links.map((link) => { const source = fixture.sources.find((item) => item.source_id === link.source_id)!; return <article className="border-2 border-ink bg-paper p-5" key={link.link_id}><div className="flex items-center justify-between gap-2"><span className={`px-2 py-1 font-mono text-[8px] font-bold uppercase ${tone[link.direction]}`}>{link.direction}</span><span className="font-mono text-[8px] font-bold uppercase text-ink/45">{source.quality} quality</span></div><h3 className="mt-4 text-lg font-black">{source.title}</h3><p className="mt-1 font-mono text-[8px] uppercase text-ink/40">{source.publisher} · {source.source_kind.replaceAll('_', ' ')}</p><blockquote className="mt-4 border-l-2 border-cobalt pl-3 text-xs italic leading-relaxed text-ink/65">{link.quoted_context}</blockquote><p className="mt-4 text-xs leading-relaxed text-ink/55">{link.relevance_note}</p><div className="mt-4 border-t border-ink/15 pt-3 font-mono text-[8px] uppercase text-ink/40">Published {source.published_at} · accessed {source.accessed_at}</div></article>; })}</div><p className="mt-4 font-mono text-[8px] font-bold uppercase text-ink/40">Fixture only · example.com links are intentionally non-evidentiary · no verdict assigned</p></div></section>;
}


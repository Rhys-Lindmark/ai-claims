import fixture from '@/data/citation-snapshots-fixture.json';
import { linkRotCount, type CitationSnapshot } from '@/lib/citation-snapshots';
export function CitationSnapshotsDemo() {
  const snapshots = fixture.snapshots as CitationSnapshot[];
  return <section className="border-t-2 border-ink bg-[#ded8ca]"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="font-mono text-[9px] font-bold uppercase text-cobalt">Citation snapshots · synthetic</p><h2 className="mt-2 text-4xl font-black tracking-[-0.05em]">Pages change. Citations shouldn’t vanish.</h2><p className="mt-3 max-w-2xl text-sm leading-relaxed text-ink/60">Retrieval time, content digest, archive location, link status, and dependent findings preserve the evidence trail after a source page moves or disappears.</p></div><span className="bg-coral px-3 py-2 font-mono text-[9px] font-bold uppercase">{linkRotCount(snapshots)} link warning</span></div><div className="mt-7 grid gap-4 lg:grid-cols-2">{snapshots.map((snapshot) => <article className="border-2 border-ink bg-paper p-5" key={snapshot.snapshot_id}><div className="flex justify-between"><span className="font-mono text-[8px] font-bold uppercase text-cobalt">{snapshot.source_id}</span><span className={`px-2 py-1 font-mono text-[7px] font-bold uppercase ${snapshot.link_status === 'live' ? 'bg-acid' : 'bg-coral'}`}>{snapshot.link_status}</span></div><h3 className="mt-5 text-2xl font-black">{snapshot.title}</h3><p className="mt-3 break-all font-mono text-[8px] text-ink/45">{snapshot.content_digest}</p><div className="mt-5 grid grid-cols-2 gap-3 text-xs"><div><span className="font-mono text-[7px] uppercase text-ink/40">Retrieved</span><strong className="mt-1 block">{snapshot.retrieved_at.slice(0, 10)}</strong></div><div><span className="font-mono text-[7px] uppercase text-ink/40">Findings</span><strong className="mt-1 block">{snapshot.dependent_finding_ids.join(' · ')}</strong></div></div><p className="mt-4 text-xs leading-relaxed text-ink/55">{snapshot.rights_note}</p></article>)}</div><p className="mt-4 font-mono text-[8px] font-bold uppercase text-ink/40">Fixture only · metadata is not a substitute for permission to reproduce source content</p></div></section>;
}

import fixture from '@/data/canonical-claim-fixture.json';
import { groupCanonicalClaims, type ClaimOccurrence } from '@/lib/canonical-claims';

export function CanonicalGroupDemo() {
  const groups = groupCanonicalClaims(fixture.occurrences as ClaimOccurrence[]);
  return <section className="border-t-2 border-ink bg-[#ded8ca]"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><p className="font-mono text-[9px] font-bold uppercase text-cobalt">Repetition guardrail</p><div className="mt-2 flex flex-wrap items-end justify-between gap-4"><h2 className="text-4xl font-black tracking-[-0.05em]">{fixture.occurrences.length} mentions ≠ {fixture.occurrences.length} claims.</h2><span className="bg-acid px-3 py-2 font-mono text-[9px] font-bold uppercase">Denominator: {groups.length}</span></div><p className="mt-4 max-w-2xl text-sm leading-relaxed text-ink/60">Exact repeats and editor-confirmed paraphrases link to one canonical review target. Every occurrence stays visible in the timeline, while the score denominator counts the underlying claim once.</p><div className="mt-7 grid gap-4 md:grid-cols-2">{groups.map((group) => <article className="border-2 border-ink bg-paper p-5" key={group.canonical_claim_id}><div className="flex items-center justify-between gap-3"><span className="font-mono text-[8px] font-bold uppercase text-coral">{group.canonical_claim_id}</span><span className="font-mono text-[8px] font-bold uppercase">Weight {group.denominator_weight}</span></div><h3 className="mt-3 text-xl font-black">{group.canonical_text}</h3><div className="mt-4 space-y-2">{group.occurrences.map((occurrence) => <div className="grid grid-cols-[42px_1fr] gap-3 border-t border-ink/15 pt-2 text-xs" key={occurrence.occurrence_id}><span className="font-mono text-ink/40">{occurrence.timestamp}</span><span><strong>{occurrence.relationship.replaceAll('_', ' ')}</strong> · {occurrence.text}</span></div>)}</div></article>)}</div></div></section>;
}


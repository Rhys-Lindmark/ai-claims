import fixture from '@/data/evidence-search-log-fixture.json';
import type { FindingSearchLog } from '@/lib/evidence-search-log';
export function EvidenceSearchDemo() {
  const log = fixture as FindingSearchLog;
  return <section className="border-t-2 border-ink bg-paper"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><p className="font-mono text-[9px] font-bold uppercase text-coral">Evidence search log · synthetic</p><h2 className="mt-2 text-4xl font-black tracking-[-0.05em]">Show how the search stopped.</h2><p className="mt-3 max-w-2xl text-sm leading-relaxed text-ink/60">Queries, databases, date bounds, filters, inclusion decisions, and stopping rationale sit beside the finding—not in an invisible research process.</p><div className="mt-7 grid gap-4 lg:grid-cols-2">{log.searches.map((search) => <article className="border-2 border-ink bg-white p-5" key={search.search_id}><div className="flex justify-between"><span className="font-mono text-[8px] font-bold uppercase text-cobalt">{search.database}</span><span className="font-mono text-[8px] uppercase text-ink/40">{search.result_count} results</span></div><p className="mt-4 border-l-4 border-acid pl-3 font-mono text-xs font-bold leading-relaxed">{search.query}</p><p className="mt-4 font-mono text-[7px] uppercase text-ink/40">Bounds: {search.date_from ?? 'no lower bound'} → {search.date_to} · {search.filters.join(' · ')}</p></article>)}</div><div className="mt-4 grid gap-3 lg:grid-cols-2">{log.decisions.map((decision) => <article className={`border-2 border-ink p-4 ${decision.decision === 'included' ? 'bg-acid' : 'bg-[#ded8ca]'}`} key={decision.decision_id}><span className="font-mono text-[8px] font-bold uppercase">{decision.decision}</span><h3 className="mt-2 text-lg font-black">{decision.title}</h3><p className="mt-2 text-xs leading-relaxed text-ink/55">{decision.reason}</p></article>)}</div><div className="mt-4 border-2 border-ink bg-ink p-5 text-paper"><p className="font-mono text-[8px] font-bold uppercase text-acid">Stopped: {log.stopping_reason.replaceAll('_', ' ')}</p><p className="mt-3 max-w-3xl text-sm leading-relaxed text-paper/65">{log.stopping_note}</p><p className="mt-3 font-mono text-[7px] uppercase text-paper/35">{log.review_state.replaceAll('_', ' ')} by {log.reviewed_by}</p></div><p className="mt-4 font-mono text-[8px] font-bold uppercase text-ink/40">Fixture only · search log documents process, not proof of completeness</p></div></section>;
}

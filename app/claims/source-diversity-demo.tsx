import fixture from '@/data/source-diversity-fixture.json'; import { diversitySummary, type SourceDiversityAudit } from '@/lib/source-diversity';
export function SourceDiversityDemo() { const audit = fixture as SourceDiversityAudit; const summary = diversitySummary(audit); const metrics = [['Sources', summary.source_count], ['Publishers', summary.publisher_count], ['Geographies', summary.geography_count], ['Source types', summary.source_type_count], ['Independent groups', summary.independence_group_count]] as const; return <section className="border-t-2 border-ink bg-acid"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><p className="font-mono text-[9px] font-bold uppercase text-cobalt">Source diversity audit · synthetic</p><h2 className="mt-2 text-4xl font-black tracking-[-0.05em]">Expose concentration. Don’t score virtue.</h2><p className="mt-3 max-w-2xl text-sm leading-relaxed text-ink/60">Publisher, geography, source type, and independence groups reveal blind spots without pretending variety itself proves accuracy.</p><div className="mt-7 grid gap-3 sm:grid-cols-5">{metrics.map(([label, value]) => <article className="border-2 border-ink bg-paper p-4" key={label}><strong className="block text-4xl">{value}</strong><span className="mt-2 block font-mono text-[8px] font-bold uppercase text-ink/45">{label}</span></article>)}</div><div className="mt-4 grid gap-4 lg:grid-cols-[.7fr_1.3fr]"><div className="border-2 border-ink bg-coral p-5"><p className="font-mono text-[8px] font-bold uppercase">Largest concentration</p><strong className="mt-2 block text-4xl">{Math.round(summary.largest_publisher_share * 100)}%</strong><p className="mt-2 text-xs leading-relaxed">of included sources share one publisher; the same maximum applies to one independence group.</p></div><div className="border-2 border-ink bg-ink p-5 text-paper"><p className="font-mono text-[8px] font-bold uppercase text-acid">Interpretation limit</p><p className="mt-3 text-sm leading-relaxed text-paper/65">{audit.limitation_note}</p><p className="mt-4 font-mono text-[7px] uppercase text-paper/35">Reviewed by {audit.reviewed_by} · {audit.finding_id}</p></div></div><p className="mt-4 font-mono text-[8px] font-bold uppercase text-ink/40">Fixture only · no diversity score · no accuracy inference</p></div></section>; }

import fixture from '@/data/episode-selection-fixture.json';
import type { EpisodeSelection } from '@/lib/episode-selection';
export function EpisodeSelectionDemo() {
  const record = fixture as EpisodeSelection;
  return <section className="border-t-2 border-ink bg-[#ffd76a]"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><p className="font-mono text-[9px] font-bold uppercase text-cobalt">Selection record · synthetic</p><h2 className="mt-2 text-4xl font-black tracking-[-0.05em]">“Latest” needs a timestamp.</h2><p className="mt-3 max-w-2xl text-sm leading-relaxed text-ink/60">Record why this episode entered the queue, what snapshot made it “latest,” whether its transcript is usable, and what the selection cannot cover.</p><div className="mt-7 grid border-2 border-ink bg-paper lg:grid-cols-[1.2fr_.8fr]"><article className="p-6 lg:border-r-2 lg:border-ink"><span className="font-mono text-[8px] font-bold uppercase text-coral">{record.selection_basis.replaceAll('_', ' ')}</span><h3 className="mt-3 text-3xl font-black leading-tight">{record.episode_title}</h3><p className="mt-4 text-sm leading-relaxed text-ink/60">{record.rationale}</p><div className="mt-6 grid gap-3 sm:grid-cols-3"><div><span className="font-mono text-[7px] uppercase text-ink/40">Catalog checked</span><strong className="mt-1 block text-sm">{record.catalog_checked_at.replace('T', ' ').slice(0, 16)} UTC</strong></div><div><span className="font-mono text-[7px] uppercase text-ink/40">Transcript</span><strong className="mt-1 block text-sm">{record.transcript_status.replaceAll('_', ' ')}</strong></div><div><span className="font-mono text-[7px] uppercase text-ink/40">Claim density</span><strong className="mt-1 block text-sm">{record.claim_density}</strong></div></div></article><aside className="bg-ink p-6 text-paper"><p className="font-mono text-[8px] font-bold uppercase text-acid">Coverage limits</p><ol className="mt-5 space-y-4">{record.coverage_limitations.map((limit, index) => <li className="flex gap-3 text-xs leading-relaxed text-paper/65" key={limit}><span className="font-mono font-bold text-coral">0{index + 1}</span><span>{limit}</span></li>)}</ol></aside></div><p className="mt-4 font-mono text-[8px] font-bold uppercase text-ink/40">Fixture only · selection explains inclusion, not truthiness</p></div></section>;
}

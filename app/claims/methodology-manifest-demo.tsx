import fixture from '@/data/methodology-manifest-fixture.json';
import type { MethodologyManifest } from '@/lib/methodology-manifest';
export function MethodologyManifestDemo() {
  const manifest = fixture as MethodologyManifest;
  return <section className="border-t-2 border-ink bg-[#ded8ca]"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="font-mono text-[9px] font-bold uppercase text-coral">Methodology manifest · synthetic</p><h2 className="mt-2 text-4xl font-black tracking-[-0.05em]">Name the rubric.</h2></div><span className="bg-ink px-3 py-2 font-mono text-[9px] font-bold uppercase text-acid">Manifest v{manifest.version}</span></div><p className="mt-3 max-w-2xl text-sm leading-relaxed text-ink/60">A published analysis points to exact versions for every stage that shapes what gets counted or concluded.</p><div className="mt-7 grid gap-3 lg:grid-cols-5">{manifest.rules.map((rule, index) => <article className="border-2 border-ink bg-paper p-4" key={rule.stage}><span className="font-mono text-[8px] font-bold uppercase text-cobalt">0{index + 1} · {rule.stage.replaceAll('_', ' ')}</span><h3 className="mt-4 text-lg font-black">{rule.rule_id}</h3><p className="mt-3 text-xs leading-relaxed text-ink/55">{rule.summary}</p><p className="mt-4 font-mono text-[7px] uppercase text-ink/40">Rule v{rule.version}</p></article>)}</div><p className="mt-4 font-mono text-[8px] font-bold uppercase text-ink/40">{manifest.content_digest} · effective {manifest.effective_at.slice(0, 10)} · fixture only</p></div></section>;
}

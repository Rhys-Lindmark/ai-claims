import traceData from '@/data/claim-transcript-trace-fixture.json';
import type { ClaimTranscriptTrace } from '@/lib/claim-transcript-trace';

export function ClaimTraceDemo() {
  const trace = traceData as ClaimTranscriptTrace;
  return <section className="border-t-2 border-ink bg-cobalt text-white"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><p className="font-mono text-[9px] font-bold uppercase text-acid">Source trace · synthetic</p><h2 className="mt-2 text-4xl font-black tracking-[-0.05em]">One claim. Its whole trail.</h2><p className="mt-3 max-w-2xl text-sm leading-relaxed text-white/65">Inspect the exact utterance without losing surrounding context, speaker uncertainty, or the canonical group that controls the denominator.</p><div className="mt-7 grid border-2 border-white lg:grid-cols-[1.35fr_.65fr]"><article className="bg-paper p-6 text-ink lg:border-r-2 lg:border-white"><div className="flex flex-wrap items-center gap-2 font-mono text-[8px] font-bold uppercase text-ink/45"><span>{trace.timestamp}</span><span>·</span><span>{trace.segment_id}</span><span>·</span><span>{trace.speaker_attribution.display_names.join(' + ')}</span></div><blockquote className="mt-5 text-lg leading-relaxed"><span className="text-ink/45">{trace.context_before} </span><mark className="bg-acid px-1 font-bold text-ink">{trace.exact_utterance}</mark><span className="text-ink/45"> {trace.context_after}</span></blockquote><div className="mt-6 border-l-4 border-coral pl-4"><p className="font-mono text-[8px] font-bold uppercase text-coral">Atomic claim</p><p className="mt-2 text-xl font-black">{trace.atomic_claim}</p></div></article><aside className="space-y-6 bg-ink p-6 text-paper"><div><p className="font-mono text-[8px] font-bold uppercase text-acid">Speaker attribution</p><strong className="mt-2 block text-xl">{trace.speaker_attribution.mode.replaceAll('_', ' ')}</strong><p className="mt-1 font-mono text-[8px] uppercase text-paper/45">{trace.speaker_attribution.confidence} confidence · {trace.speaker_attribution.review_state.replaceAll('_', ' ')}</p></div><div className="border-t border-paper/25 pt-5"><p className="font-mono text-[8px] font-bold uppercase text-acid">Canonical membership</p><strong className="mt-2 block text-xl">{trace.canonical_membership.canonical_claim_id}</strong><p className="mt-1 font-mono text-[8px] uppercase text-paper/45">{trace.canonical_membership.relationship} · {trace.canonical_membership.group_occurrence_count} occurrences</p><p className="mt-3 text-xs leading-relaxed text-paper/60">This occurrence counts through one canonical claim, preventing repetitions from multiplying the denominator.</p></div><div className="border-t border-paper/25 pt-5 font-mono text-[8px] uppercase text-paper/45">{trace.canonical_membership.grouping_state.replaceAll('_', ' ')} · {trace.trace_id}</div></aside></div><p className="mt-4 font-mono text-[8px] font-bold uppercase text-white/35">Fixture only · source trace does not itself assign a truth verdict</p></div></section>;
}

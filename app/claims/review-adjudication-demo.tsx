import fixture from '@/data/review-adjudication-fixture.json';
import { hasDisagreement, type ReviewAdjudication } from '@/lib/review-adjudication';
export function ReviewAdjudicationDemo() {
  const record = fixture as ReviewAdjudication;
  return <section className="border-t-2 border-ink bg-cobalt text-white"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="font-mono text-[9px] font-bold uppercase text-acid">Review adjudication · synthetic</p><h2 className="mt-2 text-4xl font-black tracking-[-0.05em]">Keep the disagreement.</h2><p className="mt-3 max-w-2xl text-sm leading-relaxed text-white/65">Each reviewer proposal remains visible. Editorial resolution explains the final dimensions and creates a new immutable analysis version.</p></div><span className="bg-coral px-3 py-2 font-mono text-[9px] font-bold uppercase text-ink">{hasDisagreement(record) ? 'Disagreement recorded' : 'Agreement'}</span></div><div className="mt-7 grid gap-4 lg:grid-cols-2">{record.proposals.map((proposal) => <article className="border-2 border-white/35 bg-paper p-5 text-ink" key={proposal.proposal_id}><div className="flex justify-between"><span className="font-mono text-[8px] font-bold uppercase text-cobalt">{proposal.reviewer_id}</span><span className="font-mono text-[8px] uppercase text-ink/40">{proposal.confidence} confidence</span></div><div className="mt-5 grid grid-cols-2 gap-3"><div><span className="font-mono text-[7px] uppercase text-ink/40">Accuracy</span><strong className="mt-1 block text-xl">{proposal.accuracy}</strong></div><div><span className="font-mono text-[7px] uppercase text-ink/40">Framing</span><strong className="mt-1 block text-xl">{proposal.framing.replaceAll('_', ' ')}</strong></div></div><p className="mt-4 text-xs leading-relaxed text-ink/55">{proposal.rationale}</p></article>)}</div><div className="mt-4 border-2 border-acid bg-ink p-6"><div className="flex flex-wrap justify-between gap-2"><p className="font-mono text-[8px] font-bold uppercase text-acid">Editorial resolution</p><span className="font-mono text-[8px] uppercase text-paper/40">{record.resulting_analysis_version_id}</span></div><p className="mt-4 max-w-3xl text-sm leading-relaxed text-paper/70">{record.resolution}</p><div className="mt-5 flex gap-3"><strong className="bg-acid px-3 py-2 text-sm text-ink">Accuracy: {record.final_accuracy}</strong><strong className="bg-coral px-3 py-2 text-sm text-ink">Framing: {record.final_framing?.replaceAll('_', ' ')}</strong></div></div><p className="mt-4 font-mono text-[8px] font-bold uppercase text-white/35">Fixture only · resolution does not erase minority rationale</p></div></section>;
}

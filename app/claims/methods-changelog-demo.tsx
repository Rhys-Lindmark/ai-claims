import fixture from '@/data/methods-changelog-fixture.json';
import { affectedAnalysisCount, type MethodChange } from '@/lib/methods-changelog';
export function MethodsChangelogDemo() {
  const changes = fixture.changes as MethodChange[];
  return <section className="border-t-2 border-ink bg-white"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="font-mono text-[9px] font-bold uppercase text-cobalt">Methods changelog · synthetic</p><h2 className="mt-2 text-4xl font-black tracking-[-0.05em]">Say what changed—and who it affects.</h2><p className="mt-3 max-w-2xl text-sm leading-relaxed text-ink/60">Breaking scoring changes trigger affected-analysis tracking. Clarifications remain visibly distinct and never imply silent recomputation.</p></div><div className="border-2 border-ink bg-coral px-4 py-3 text-center"><strong className="block text-3xl">{affectedAnalysisCount(changes)}</strong><span className="font-mono text-[7px] font-bold uppercase">analyses affected</span></div></div><div className="mt-7 grid gap-4 lg:grid-cols-2">{changes.map((change) => <article className="border-2 border-ink bg-paper p-5" key={change.change_id}><div className="flex flex-wrap items-center justify-between gap-2"><span className={`px-2 py-1 font-mono text-[8px] font-bold uppercase ${change.kind === 'breaking_scoring_change' ? 'bg-coral' : 'bg-acid'}`}>{change.kind.replaceAll('_', ' ')}</span><span className="font-mono text-[8px] uppercase text-ink/40">v{change.from_manifest_version} → v{change.to_manifest_version}</span></div><h3 className="mt-5 text-2xl font-black leading-tight">{change.summary}</h3><p className="mt-3 text-xs leading-relaxed text-ink/55">{change.rationale}</p><div className="mt-4 flex flex-wrap gap-2">{change.affected_stages.map((stage) => <span className="border border-ink/20 px-2 py-1 font-mono text-[7px] font-bold uppercase" key={stage}>{stage.replaceAll('_', ' ')}</span>)}</div><div className="mt-5 border-l-4 border-cobalt pl-3"><p className="text-xs leading-relaxed">{change.public_notice}</p><p className="mt-2 font-mono text-[7px] font-bold uppercase text-cobalt">Recompute: {change.recomputation_state} · {change.affected_analysis_ids.length} analyses</p></div></article>)}</div><p className="mt-4 font-mono text-[8px] font-bold uppercase text-ink/40">Fixture only · prior manifests and analysis versions remain addressable</p></div></section>;
}

import fixture from '@/data/limitations-register-fixture.json';
import { affectedFindings, type AnalysisLimitation } from '@/lib/limitations-register';
export function LimitationsRegisterDemo() {
  const limitations = fixture.limitations as AnalysisLimitation[];
  return <section className="border-t-2 border-ink bg-[#ffd76a]"><div className="mx-auto max-w-6xl px-5 py-12 md:px-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="font-mono text-[9px] font-bold uppercase text-cobalt">Limitations register · synthetic</p><h2 className="mt-2 text-4xl font-black tracking-[-0.05em]">Known limits, linked to consequences.</h2><p className="mt-3 max-w-2xl text-sm leading-relaxed text-ink/60">Transcript, selection, evidence, and methodology limits name the findings they affect and whether mitigation is open, accepted, or complete.</p></div><div className="border-2 border-ink bg-paper px-4 py-3 text-center"><strong className="block text-3xl">{affectedFindings(limitations)}</strong><span className="font-mono text-[7px] font-bold uppercase">findings affected</span></div></div><div className="mt-7 grid gap-4 lg:grid-cols-3">{limitations.map((item) => <article className="border-2 border-ink bg-paper p-5" key={item.limitation_id}><div className="flex justify-between"><span className="font-mono text-[8px] font-bold uppercase text-cobalt">{item.kind}</span><span className={`px-2 py-1 font-mono text-[7px] font-bold uppercase ${item.status === 'open' ? 'bg-coral' : item.status === 'mitigated' ? 'bg-acid' : 'bg-[#ded8ca]'}`}>{item.status}</span></div><h3 className="mt-5 text-xl font-black">{item.title}</h3><p className="mt-3 text-xs leading-relaxed text-ink/55">{item.description}</p><div className="mt-4 border-l-4 border-cobalt pl-3"><p className="font-mono text-[7px] font-bold uppercase text-cobalt">Mitigation</p><p className="mt-2 text-xs leading-relaxed">{item.mitigation}</p></div><p className="mt-4 font-mono text-[7px] uppercase text-ink/40">{item.severity} · {item.affected_finding_ids.join(' · ')}</p></article>)}</div><p className="mt-4 font-mono text-[8px] font-bold uppercase text-ink/40">Fixture only · accepted does not mean resolved · prior versions remain visible</p></div></section>;
}

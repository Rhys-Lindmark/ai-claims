import compatibility from '@/extension/data/compatibility.json';
import attestation from '@/extension/data/deployment-attestation.json';

const columns = [
  ['Detection', 'detection'],
  ['Canonical identity', 'identity'],
  ['Acquisition', 'acquisition'],
  ['Score states', 'score_states'],
  ['Evidence', 'evidence_destination'],
  ['Current proof', 'proof'],
] as const;

export function ExtensionCompatibility() {
  return <section className="border-b-2 border-ink bg-paper"><div className="mx-auto max-w-6xl px-5 py-10 md:px-10"><p className="font-mono text-[9px] font-bold uppercase text-coral">Page compatibility · schema 1.0.0</p><h2 className="mt-3 text-4xl font-black leading-[.94] tracking-[-.045em]">SAME GATE. DIFFERENT SOURCE RULES.</h2><p className="mt-4 max-w-2xl text-sm leading-relaxed text-ink/60">Recognition is not permission to acquire content. Every surface shares canonical lookup and score suppression, while its source and evidence path remain explicit.</p><div className="mt-7 overflow-x-auto border-2 border-ink"><table className="min-w-[1050px] border-collapse text-left"><thead><tr className="bg-ink text-paper"><th className="p-3 font-mono text-[8px] uppercase">Surface</th>{columns.map(([label]) => <th className="border-l border-paper/30 p-3 font-mono text-[8px] uppercase" key={label}>{label}</th>)}</tr></thead><tbody>{compatibility.surfaces.map((surface) => <tr className="border-t-2 border-ink align-top" key={surface.kind}><th className="bg-white p-3 text-xl font-black">{surface.label}<span className="mt-2 block font-mono text-[7px] uppercase text-cobalt">{surface.proof_level.replaceAll('_', ' ')}</span></th>{columns.map(([, key]) => <td className="border-l border-ink/25 p-3 text-[11px] leading-relaxed" key={key}>{surface[key]}</td>)}</tr>)}</tbody></table></div><p className="mt-3 font-mono text-[8px] font-bold uppercase text-ink/45">Resolver behavior for all three: {compatibility.surfaces[0].resolver}.</p><div className="mt-8 border-2 border-ink bg-white p-5 shadow-[5px_5px_0_#1c1c1a]"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="font-mono text-[8px] font-bold uppercase text-cobalt">Latest production attestation · pass</p><h3 className="mt-1 text-2xl font-black">3/3 SURFACES VERIFIED</h3><p className="mt-2 font-mono text-[7px] font-bold uppercase text-ink/45">Sites v{attestation.tested_site_version} · <time>{attestation.verified_at}</time> · commit {attestation.tested_commit_sha.slice(0, 7)}</p></div><div className="flex flex-col items-start gap-2 sm:items-end"><code className="bg-ink px-3 py-2 font-mono text-[8px] text-acid">{attestation.verification_command}</code><a className="font-mono text-[8px] font-bold uppercase text-cobalt underline" href="https://ai.rhyslindmark.com/claims/api/v1/deployment-attestations">Open current JSON →</a></div></div><div className="mt-4 grid gap-2 sm:grid-cols-3">{attestation.results.map((result) => <a className="border-2 border-ink bg-paper p-3 transition-transform hover:-translate-y-0.5" href={result.evidence_url} key={result.kind}><span className="font-mono text-[7px] font-bold uppercase text-coral">{result.kind} · HTTP {result.resolver_status}/{result.route_status}</span><strong className="mt-2 block text-3xl font-black text-cobalt">{result.observed_score}/100</strong><span className="mt-2 block font-mono text-[7px] uppercase text-ink/45">Expected = observed · {result.reviewed_claims}/{result.eligible_claims} reviewed →</span></a>)}</div><p className="mt-4 border-t border-ink/15 pt-3 font-mono text-[7px] font-bold uppercase text-ink/45">SHA-256 {attestation.integrity.digest_hex.slice(0, 16)}… · visitor data collected: no · retained visitor fields: 0</p></div></div></section>;
}
